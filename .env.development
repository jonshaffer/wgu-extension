# Development mode - automatically loaded during `wxt` (dev server)
# Use local JSON files for faster development and offline work
WXT_USE_LOCAL_COURSE_FILES=true
WXT_FORCE_EXTENSION_CONTEXT=false

# Development-specific settings
WXT_DEBUG_MODE=true

name: WXT Extension CI

on:
  push:
    branches: [ main, develop, feat/* ]
    paths:
      - 'extension/**'
      - '!extension/**/*.md'
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
      - '.github/ISSUE_TEMPLATE/**'
      - 'functions/**'
      - 'site/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'extension/**'
      - '!extension/**/*.md'
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
      - '.github/ISSUE_TEMPLATE/**'
      - 'functions/**'
      - 'site/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.14.0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build required packages
        run: pnpm run build:libs

      - name: Type check
        run: pnpm run typecheck --filter=extension

      - name: Lint
        run: pnpm run lint --filter=extension

      - name: Test development build
        run: pnpm run build:dev --filter=extension

      - name: Test production build
        run: pnpm run build:prod --filter=extension

  build-preview:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/heads/feat/')
    needs: test
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.14.0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build required packages
        run: pnpm run build:libs

      - name: Build preview
        run: pnpm run build:preview --filter=extension

      - name: Create preview ZIPs
        run: |
          pnpm run zip:preview --filter=extension
          pnpm run zip:firefox:preview --filter=extension
          pnpm run zip:edge:preview --filter=extension
      
      - name: Upload preview artifacts
        uses: actions/upload-artifact@v4
        with:
          name: extension-preview
          path: extension/.output/*.zip
          retention-days: 7

  build-production:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: test
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.14.0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build required packages
        run: pnpm run build:libs

      - name: Build production
        run: pnpm run build:prod --filter=extension

      - name: Create production ZIPs
        run: |
          pnpm run zip --filter=extension
          pnpm run zip:firefox --filter=extension
          pnpm run zip:edge --filter=extension
      
      - name: Upload production artifacts
        uses: actions/upload-artifact@v4
        with:
          name: extension-production
          path: extension/.output/*.zip
          retention-days: 7



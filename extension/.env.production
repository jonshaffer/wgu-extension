# Production mode - automatically loaded during `wxt build`
# Use extension URLs for proper production behavior
WXT_USE_LOCAL_COURSE_FILES=false
WXT_FORCE_EXTENSION_CONTEXT=false

# Production-specific settings
WXT_DEBUG_MODE=false

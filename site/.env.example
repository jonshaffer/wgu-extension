# GraphQL API Configuration
# Set to "production" to use the production GraphQL endpoint during development
# Set to "local" or leave empty to use the local emulator
VITE_GRAPHQL_ENV=local

# Optional: Override the GraphQL endpoint URL entirely
# VITE_GRAPHQL_ENDPOINT=http://localhost:5001/wgu-extension/us-central1/graphql/graphql

# Firebase Configuration for WGU Extension Site
# Required for admin authentication and Firebase services

# Firebase Project Configuration
VITE_FIREBASE_API_KEY="your-api-key-here"
VITE_FIREBASE_AUTH_DOMAIN="wgu-extension-site-prod.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="wgu-extension-site-prod"
VITE_FIREBASE_STORAGE_BUCKET="wgu-extension-site-prod.appspot.com"
VITE_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
VITE_FIREBASE_APP_ID="your-app-id"
VITE_FIREBASE_MEASUREMENT_ID="your-measurement-id"

# Development Settings
# In development, the Firebase Auth emulator will be used automatically
# Functions will point to local emulator at http://localhost:5001
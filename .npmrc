# Global pnpm configuration for WGU Extension monorepo
# Ensure optional dependencies are installed in all environments
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
fund=false
audit=false

# For CI/CD environments, ensure platform-specific dependencies are properly resolved
save-exact=false